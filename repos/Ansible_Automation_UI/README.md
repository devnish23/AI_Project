# Ansible Automation UI

This is a sample Flask-based UI that demonstrates simple RBAC for infrastructure automation.

## Features
- **Admin** can generate Ansible playbooks using a local language model.
- **Operator** can onboard devices, test connectivity, and execute playbooks created by the admin.

This project uses a small text-generation model for demonstration purposes and does not interact with real infrastructure.
