# Ansible Automation Frontend

This sample frontend is a simple React application served via CDN.
It demonstrates RBAC with **Admin** and **Operator** roles.
The backend API is expected to expose routes similar to the included `fastapi_backend.py`.

Open `index.html` in a browser or serve it from a static HTTP server.
